# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Dependencies and generated output.
node_modules/
dist/
coverage/
.tsbuild/
*.tsbuildinfo
benchmarks/results/
benchmarks/test-results/

# External tooling checkout. It contains a second copy of this repository,
# including a nested biome.json, so it is excluded from git and from Biome.
.freebuff/

# Private development and agent context. These files remain available in the
# maintainer workspace but are intentionally excluded from the public repository.
AGENTS.md
CHECKLIST.md
.github/copilot-instructions.md
/docs/
/templates/
/tests/
