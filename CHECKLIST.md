# Ordered Implementation Checklist

Statuses: `PROPOSED`, `READY`, `IN_PROGRESS`, `BLOCKED`, `DONE`.

An agent may select only one `READY` task whose dependencies are all `DONE`. Completion evidence must include commands, results, affected acceptance criteria, and security impact.

## M0 — Discovery, governance, and decisions

- [x] **FW-001 [DONE/P0] Validate the PRD and glossary** — started 2026-08-15, completed 2026-08-15, agent: GitHub Copilot (`gpt-pro`)
  - AC: terminology and requirement IDs are consistent; conflicts are recorded.
  - Evidence: complete manual audit and resolved conflict ledger in `docs/PRD_VALIDATION_REPORT.md`; canonical terminology in `docs/GLOSSARY.md`; explicit requirement and acceptance-criterion mappings in `docs/14_REQUIREMENTS_TRACEABILITY.md`.
  - AC result: terminology consistency PASS; requirement/reference consistency PASS; conflict recording PASS.
  - Resolution: preserve the Master PRD by delivering minimal opt-in client navigation in v0.1; v0.2 contains enhancements and hardening. The protocol remains gated by FW-301's ADR.
  - Security impact: low, documentation/governance only; no executable trust boundary changed. P0 security evidence remains a required future traceability gate.
  - Verification: Markdown diagnostics report no errors; manual namespace/reference audit passes; all numeric traceability tasks exist; no dependency or executable toolchain applies.
- [x] **FW-002 [DONE/P0] Create the monorepo skeleton** — depends FW-001 (DONE); promoted to `READY` 2026-08-15 under delegated autonomy, started 2026-08-15, completed 2026-08-15, agent: GitHub Copilot (`gpt-pro`)
  - AC: workspace, formatting, lint, type checking, testing, and CI skeleton exist without feature implementation.
  - Governing decisions: `Accepted` ADR-001 (`docs/adr/ADR-001-codename-and-package-scope.md`) and `Accepted` ADR-005 (`docs/adr/ADR-005-monorepo-and-toolchain.md`).
  - Evidence: `pnpm-workspace.yaml`, `package.json`, `tsconfig.base.json`, `tsconfig.json`, `biome.json`, `vitest.config.ts`, `.github/workflows/ci.yml`, `tests/workspace-skeleton.test.ts`, `.gitignore`.
  - AC result: workspace PASS; formatting PASS; lint PASS; type checking PASS; testing PASS; CI skeleton PASS; no feature implementation PASS (`packages/` and `examples/` contain no code, asserted by test).
  - Verification: `pnpm install` exit 0; `pnpm run format:check` exit 0; `pnpm run lint` exit 0; `pnpm run typecheck` exit 0; `pnpm run test` exit 0 with 10 passing tests; `pnpm run verify` exit 0. Toolchain: node v24.16.0, pnpm 11.5.1, Corepack 0.35.0.
  - Security impact: low. Development dependencies only, exact pinned versions, frozen-lockfile installs in CI, dependency install scripts denied by default with a justified `allowBuilds` exception list, read-only workflow permissions, no secrets, no publish workflow, all packages private.
  - Residual: version control is not initialized in this working copy, so commit-review, provenance, and release controls (`SEC-SUPPLY-002`, `SEC-SUPPLY-003`) cannot be exercised yet. The CI workflow is defined but unexecuted for the same reason.
- [x] **FW-003 [DONE/P0] Decide the first renderer** — depends FW-001 (DONE); promoted to `READY` 2026-08-15 under delegated autonomy, started 2026-08-15, completed 2026-08-15, agent: GitHub Copilot (`gpt-pro`)
  - AC: at least two spikes compare SSR, hydration, bundle size, DX, maintenance, and license.
  - Governing decision: `Accepted` ADR-002 (`docs/adr/ADR-002-first-official-renderer.md`) selects the small independent renderer (`preact` + `preact-render-to-string`) as the first official renderer, reached only through the FW-111 contract, with a React-compatible renderer as planned follow-on work.
  - Evidence: `spikes/renderer-evaluation/` (throwaway measurement spike) and its generated artifacts `results/renderer-comparison.md` and `results/renderer-comparison.json`.
  - AC result: PASS with three candidates rather than the required two — `react` + `react-dom`, `preact` + `preact-render-to-string`, and the React-shaped API on the small runtime (`preact/compat`). SSR PASS; hydration PASS; bundle size PASS; DX PASS; maintenance PASS; license PASS.
  - Key measurements (Node v24.16.0, win32-x64, esbuild 0.28.2, 200 timed renders after 20 warm-ups): client hydration bundle gzip 60 891 B (react) / 5 960 B (preact) / 8 109 B (compat), a 10.2x spread between the first two; brotli 52 331 B / 5 389 B / 7 366 B; server bundle 557 101 B / 31 559 B / 39 308 B; runtime package graph 3 / 2 / 2, all MIT. Hydration proven functionally for all three by clicking the rehydrated button and asserting the counter text changed (`Clicks: 0` -> `Clicks: 1`). Static page shipped 0 client bytes with no `<script` for all three, satisfying AC-PROD-04. `react-dom/server` required a `createRequire(import.meta.url)` banner to bundle to ESM at all; both small-runtime candidates are ESM-native.
  - Security evidence: a hostile string (`"><script>alert(1)</script><img src=x onerror=alert(2)>`) rendered in text, attribute, and URL position was escaped by all three candidates such that no `script` or `img` element was created (`SEC-XSS-001`, `SEC-XSS-003`). The harness exits non-zero if hydration, the zero-JS guarantee, or escaping regresses, so these are enforced rather than observed once. Maintenance evidence from the registry: react/react-dom 465 releases in 12 months with 2 npm maintainers; preact 22 with 6; preact-render-to-string 9 with 6.
  - Verification: `pnpm install` exit 0; spike harness completed with `hydration interactive=true` for all three candidates; `pnpm run check` clean over 18 files; `pnpm run verify` exit 0 with 11 passing tests (a new test asserts every `spikes/*` package is `private`, carries no runtime dependencies, and does not occupy the `@nusajs/` scope); `get_errors` reports none.
  - Security impact: low. The spike added six development dependencies (`esbuild`, `happy-dom`, `preact`, `preact-render-to-string`, `react`, `react-dom`), all exact-pinned and all MIT-verified from the registry before adoption. No install script was granted; the `allowBuilds: { esbuild: false }` denial from ADR-005 still holds even though esbuild is now a direct spike dependency (`SEC-SUPPLY-005`). No framework runtime dependency was introduced.
  - Residual: SSR throughput is not reproducible on this machine — repeated runs moved the react-to-preact ratio across roughly 1.2x to 2.1x, so no throughput claim from this spike is publishable and ADR-002 rests on bundle size, module format, dependency count, and escaping instead. A defensible number requires FW-007. The escaping probe is a single payload, not the XSS polyglot corpus required by `docs/09_SECURITY_PRD.md`; that corpus is owned by FW-112 and renderer conformance. Spike sources are `.mjs`/`.jsx` outside the root `tsconfig.json` include list and so are not type-checked, which must not be true of FW-111 or FW-112. Version control is still not initialized, so `SEC-SUPPLY-002` and `SEC-SUPPLY-003` remain unexercised.
- [x] **FW-004 [DONE/P0] Decide route convention and module API** — depends FW-001 (DONE); promoted to `READY` 2026-08-16 under delegated autonomy, started and completed 2026-08-16, agent: GitHub Copilot (`gpt-pro`)
  - AC (derived; the checklist line carries no AC sub-bullet, so acceptance is taken from the governing documents): AC-1 the filesystem convention is decided by an `Accepted` ADR, because `docs/03_ROUTING_AND_NAVIGATION.md` requires that "the final convention requires an ADR after a prototype evaluates discoverability, collisions, generated types, and cross-platform filesystem behavior"; AC-2 the route-module API syntax is decided by an `Accepted` ADR, closing the `docs/00_MASTER_PRD.md` section 15 open decision; AC-3 at least two architecture paths are compared with evidence, per the `docs/12_ROADMAP_AND_RELEASES.md` M0 exit; AC-4 the chosen module API is readable at build time without executing application modules, per `docs/06_COMPILER_AND_DEV_SERVER.md`; AC-5 precedence is decidable from patterns rather than filesystem enumeration order, per `docs/03_ROUTING_AND_NAVIGATION.md`; AC-6 the decisions are propagated into the Master PRD, the decisions register, and the traceability matrix in the same change, and are held in place by an automated test.
  - Governing decisions: `Accepted` ADR-003 (`docs/adr/ADR-003-route-filesystem-convention.md`) selects role-suffixed filenames (Option A) with three binding amendments — A1 one canonical spelling per URL, A2 case-folded and NFC-normalised collision comparison, A3 reserved-device-name rejection at scan time. `Accepted` ADR-004 (`docs/adr/ADR-004-route-module-api-syntax.md`) selects the hybrid (Option C): analyzable named exports are normative, typed helpers are optional, and the compiler never requires a helper.
  - Evidence: `spikes/route-convention/` (throwaway measurement spike, `@nusajs-spike/route-convention`, private, no runtime dependencies) and its generated artifacts `results/route-convention-comparison.md` and `results/route-convention-comparison.json`. The harness is enforcing rather than reporting: it exits non-zero when a candidate fails a hard requirement.
  - AC-1 PASS: ADR-003 is `Accepted` and compares three conventions against one shared, convention-independent fixture set of 9 logical routes, 4 boundaries, and 5 collision cases, covering discoverability (reserved-name count), aliasing (spellings per URL), collisions, precedence, and a real cross-platform filesystem probe.
  - AC-2 PASS: ADR-004 is `Accepted`; `docs/00_MASTER_PRD.md` section 15 now carries the route-module syntax line struck through and resolved, and `docs/13_POSITIONING_RISKS_AND_DECISIONS.md` promotes the reserved IDs ADR-003 and ADR-004 to `Accepted` files.
  - AC-3 PASS with three candidates rather than the required two — `suffix` (role in the filename), `folder` (role in a fixed filename inside a route folder), and `manifest` (explicit declaration, no filesystem meaning) — plus two candidate module APIs, named exports and a `definePage()` wrapper.
  - AC-4 PASS: measured with pure-syntax analysis (`ts.createSourceFile`, no `ts.createProgram`, no type checker, no module execution). Named exports read a literal config in 2 analyzer steps with no cross-module resolution and produce a diagnostic rather than a guess for a computed value. The wrapper needs 5 steps and cross-module binding resolution, and a name-only matcher silently misses `import { definePage as dp }` — a measured `not-found`, which is why the wrapper is optional and never required.
  - AC-5 PASS: 0 overlapping pattern pairs tie on specificity for any of the three conventions, so precedence is computed from the parsed pattern in every case; the `manifest` candidate is the only one whose precedence source is declaration order, which is one reason it was not chosen.
  - AC-6 PASS: `tests/route-convention.test.ts` (35 tests) asserts template structure, `Accepted` status, three compared options, spike citation, and that every `FW-`, `SEC-`, and `AC-ROUTE-` identifier in both ADRs resolves against `CHECKLIST.md`, `docs/09_SECURITY_PRD.md`, and `docs/03_ROUTING_AND_NAVIGATION.md`; it re-asserts the measured facts the ADRs cite and the three propagation edits.
  - Key measurements (Node v24.16.0, win32, TypeScript 5.9.3): reserved names 11 / 10 / 1 for suffix / folder / manifest; maximum spellings per URL 2 / 1 / 1 and total spellings 17 / 9 / 9; collisions reported 5 of 5, 4 of 4, and 5 of 5, always naming every conflicting file, satisfying the AC-ROUTE-02 shape; the one `folder` case not counted is `duplicate-static`, which that convention cannot express at all rather than failing to detect. Filesystem probe: 26 candidate paths created, 0 rejected, case-insensitive path resolution `true`, NFC/NFD folding `false`, and all five reserved device names (`con`, `nul`, `aux`, `prn`, `com1`) creatable in all three positions.
  - Security evidence: none. Both ADRs analyse `SEC-INPUT-001`, `SEC-INPUT-003`, `SEC-INPUT-004`, `SEC-INPUT-005`, `SEC-FILE-003`, the `SEC-SECRET-*` family, and `SEC-SUPPLY-005`, and place concrete obligations on FW-104, FW-105, FW-107, and FW-119 — but per `docs/SECURITY_EVIDENCE_WORKFLOW.md` section 3.2 no requirement is discharged by a decision, so this task discharges nothing. The case-insensitive/NFC-folding split is the measured basis for amendment A2, and the device-name result is the measured basis for A3.
  - Verification: `pnpm run format` exit 0; `pnpm run verify` exit 0 — `format:check` and `lint` clean over 33 files, `typecheck` clean, `Test Files 6 passed (6)` with 124 tests passing, `No known vulnerabilities found`, and `License gate passed: 68 packages across 6 license expressions`. The spike harness completed with exit 0 and `failures: []`. `graft build` rebuilt the graph to 116 nodes, 209 edges, 27 cards. The first `verify` run failed with 7 assertions in the new test, all of which were assertion errors rather than document errors: three JSON field names differed from the shape the test assumed, one quoted requirement straddled a line wrap, and the markdown heading checks were CRLF-sensitive; line endings are now normalised on read.
  - Security impact: medium by decision content, none by change surface. No dependency was added. Two root files changed for tool hygiene: `biome.json` and `.gitignore` now exclude `.freebuff/`, an external tooling checkout that contains a second copy of this repository including a nested `biome.json`, which made Biome refuse to run at all ("Found a nested root configuration"). That exclusion narrows tool discovery to this repository and does not weaken any rule.
  - Residual: the filesystem probe ran on `win32` only, so ADR-003's A2 and A3 rationale may not be cited for macOS or Linux until the harness is re-run there; the expectation that case-insensitivity holds on default macOS and not on Linux is stated in the ADR as unmeasured. The spike's two analyzers are roughly 40-line models, so re-export chains, `export { route } from "./shared"`, namespace imports, and declaration merging are unmeasured and are open questions for FW-105. `AC-ROUTE-01` through `AC-ROUTE-07` all remain open and are owned by M1 tasks; a decision cannot satisfy them. ADR-008 remains `Proposed`, so strict-mode configuration is still not accepted authority. Version control is in fact now initialized with one commit and an `origin/main` remote, which contradicts accepted risk `AR-001` ("git uninitialized", status `RENEWED`); that register entry needs a closing amendment under `docs/SECURITY_EVIDENCE_WORKFLOW.md` section 4, which is outside this task's scope and is left for the next task that touches the register.
- [x] **FW-005 [DONE/P0] Decide toolchain and support policy** — depends FW-002 (DONE); promoted to `READY` 2026-08-17 under delegated autonomy, started and completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - AC (derived; the checklist line states an outcome rather than criteria): AC-1 a written support policy exists and is linked from the traceability matrix. AC-2 an accepted ADR governs it and compares at least three options. AC-3 the supported runtime set, the declared `engines.node` floor, and the continuous-integration matrix agree, and the agreement is asserted mechanically. AC-4 the supported TypeScript range is stated and derived from the repository's own compiler options. AC-5 no end-of-support window is published as a guarantee the maintainer team cannot sustain. AC-6 the policy names an owner for everything it declares out of scope and discharges no `SEC-*` requirement it does not implement.
  - Governing decisions: `Accepted` ADR-006 (`docs/adr/ADR-006-supported-runtime-and-typescript-policy.md`) selects Option B — the Node floor is the oldest LTS line still inside its official upstream support window — over keeping the widest floor (Option A), tracking only the current LTS (Option C), and declaring no floor at all (Option D). Eleven binding commitments C1 to C11: floor `22.12.0`, primary line 24.x, mechanical agreement between the four artifacts, drop-only-at-upstream-EOL, three runtime tiers, TypeScript floor 5.8, informational pre-release compilers, emitted types as public API, pnpm primary, end-of-support windows as targets, and browser support excluded and assigned to ADR-012.
  - Evidence: `docs/SUPPORT_POLICY.md` (10 sections) is the policy; `tests/support-policy.test.ts` (23 tests) enforces it; `.github/workflows/ci.yml` and `package.json` are the enforced artifacts.
  - AC-1 PASS: `docs/SUPPORT_POLICY.md` exists and is cited from three rows of `docs/14_REQUIREMENTS_TRACEABILITY.md` — `NFR-002`, `NFR-010`, and `AC-ARCH-01–06` — each naming the ADR, the policy, and the enforcing test.
  - AC-2 PASS: ADR-006 is `Accepted`, carries every heading of `templates/ADR_TEMPLATE.md`, and compares four options; `docs/13_POSITIONING_RISKS_AND_DECISIONS.md` promotes the reserved ADR-006 line to an `Accepted` file in the same form as ADR-003 to ADR-005.
  - AC-3 PASS: `package.json` `engines.node` is `>=22.12.0`, the section 3.1 table marks 22.x and 24.x supported and 20.x end-of-life, and the tier-1 matrix is `["22.12.0", "24"]` across three operating systems. Four assertions enforce this: the floor equals `engines.node`, every supported major is gated, no unsupported major is gated, and the floor is exercised at its exact minimum rather than at the major so a matrix entry of `22` cannot silently resolve to the newest 22.x.
  - AC-4 PASS: the floor is TypeScript 5.8, derived from `erasableSyntaxOnly` in `tsconfig.base.json`, which does not exist before 5.8. The test asserts the floor is at or below the pinned 5.9.3 and that the option the derivation rests on is still present, so removing it fails the build rather than leaving a stale floor standing.
  - AC-5 PASS: section 6.1 marks the previous-major window an internal target and not a guarantee, names FW-709 as the owner of publishing it, and permits promotion only after one backport cycle has actually been executed. This is `docs/09_SECURITY_PRD.md` section 24's closing sentence applied rather than restated.
  - AC-6 PASS: section 8 assigns browser support to ADR-012, the manifest schema to ADR-008, adapter capability maps to FW-113, FW-211, and FW-214, publish mechanics to `docs/SUPPLY_CHAIN_POLICY.md`, and the universal-package boundary scanner to FW-117; section 9 names an authority for each of four manual gates. The test asserts every `FW-` and `SEC-` identifier in both documents resolves against `CHECKLIST.md` and `docs/09_SECURITY_PRD.md`.
  - Key measurements: the Node LTS schedule was fetched from `nodejs/Release` rather than recalled — 20.x ended 2026-04-30, 22.x ends 2027-04-30, 24.x ends 2028-04-30, 26.x enters LTS 2026-10-28. That single fact is what settles the decision: the previous `engines.node` of `>=20.19.0` was not merely inconsistent with the CI matrix and with ADR-005's validated local toolchain, it advertised a runtime that stopped receiving security patches three and a half months ago. The tier-1 job count doubles from three to six.
  - Security evidence: none. The policy implements no control, so per `docs/SECURITY_EVIDENCE_WORKFLOW.md` section 3.2 it discharges nothing; `SEC-SUPPLY-001`, `SEC-SUPPLY-002`, and `SEC-SUPPLY-003` remain owned elsewhere and still cite `AR-001`. What the task does produce is a constraint on their future evidence: because the matrix is now a function of the claimed support set, a supported platform cannot go unscanned while remaining advertised.
  - Verification: `pnpm run format` exit 0; `pnpm run verify` exit 0 — `format:check` and `lint` clean over 34 files, `typecheck` clean, `Test Files 7 passed (7)` with 147 tests passing, `No known vulnerabilities found`, `License gate passed: 68 packages across 6 license expressions`. The first `verify` run failed with 2 assertions in the new test, both assertion errors rather than document errors: the test guessed at wording the policy states differently ("discharges no `SEC-`" versus "no `SEC-*` requirement is discharged by this document", and lower-case authority names versus the table's "Core maintainers" and "Pull-request approver").
  - Security impact: medium. The change surface is four artifacts and no dependency, but raising `engines.node` off an end-of-life line is itself the security-relevant act: the abuse case is a consumer deploying onto an unpatched runtime because the project's own machine-readable metadata told them it was supported.
  - Residual: no continuous-integration run has been observed, so the tier-1 matrix and the informational TypeScript canary job are defined gates and not evidence — `AR-001` covers that, and this task amended it. `AR-001`'s scope was factually wrong ("version control is not initialized"); it is corrected here as an `Amended:` field rather than a renewal, because correction is not one of the three lifecycle actions in `docs/SECURITY_EVIDENCE_WORKFLOW.md` section 6.4 and must not consume the one renewal remaining before mandatory escalation. The same stale clause was corrected in `docs/SUPPLY_CHAIN_POLICY.md` section 13, `docs/STRICT_SECURITY_MODE.md` sections 7 and 10, and `docs/SECURITY_THREAT_MODEL_APPROVAL.md` section 7, and recorded as amendment A-4 on that approval record. Nothing type-checks against TypeScript 5.8, so that floor is a derivation and not a measurement. The 22.12.0 `require(esm)` justification was taken from release documentation, not executed locally; the local toolchain is Node v24.16.0. The npm and Yarn tiers are stated and ungated until FW-115 and FW-215 create a project to install. The section 2 review cadence is a manual process control with no automated trigger, so the support table can become false without any test failing.
- [x] **FW-006 [DONE/P0] Define error taxonomy** — depends FW-001 (DONE); promoted to `READY` 2026-08-17 under delegated autonomy, started and initially blocked 2026-08-17, unblocked by core-maintainer acceptance of ADR-007 and completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - AC (derived from accepted ADR-007): AC-1 canonical grammar and the closed subsystem vocabulary are explicit; AC-2 allocation is monotonic per subsystem with duplicate rejection and permanent stable and experimental tombstones; AC-3 every registry record has code, subsystem, summary, severity, stability, deterministic documentation slug, and status; AC-4 route collision, invalid configuration, missing capability, unsafe relaxation, internal invariant, and production request-failure candidates demonstrate subsystem boundaries; AC-5 parser exercises reject lowercase, unknown subsystem, `0000`, overflow, dynamic suffixes, and third-party impersonation; AC-6 security diagnostics cannot be informational or suppressible, static identifiers carry no attacker data, and registry membership is not treated as production-safety evidence.
  - Evidence: accepted `docs/adr/ADR-007-error-code-taxonomy.md`; allocation policy and candidate rationale in `docs/ERROR_CODE_TAXONOMY.md`; machine-readable `docs/error-codes.json` constrained by `docs/error-codes.schema.json`; eight conformance exercises in `tests/error-taxonomy.test.ts`; FR-010 evidence updated in `docs/14_REQUIREMENTS_TRACEABILITY.md`.
  - AC result: AC-1 PASS for `^NUSA-[A-Z][A-Z0-9]{1,11}-[0-9]{4}$`, `0001`–`9999`, and eleven registered subsystems; AC-2 PASS with ordered per-subsystem validation, global uniqueness, and retired experimental `NUSA-ROUTE-0002`; AC-3 PASS with identity/slug checks over every record; AC-4 PASS with six active candidates spanning six subsystems; AC-5 PASS with all required malformed examples; AC-6 PASS as a governance constraint, including severity tests and explicit non-claim language.
  - Architecture: no new runtime or public TypeScript API was introduced. The task implements the accepted namespace and repository allocation authority only. FW-102 still owns runtime diagnostic types, mechanical production validator, complete documentation URLs, formatters, JSON snapshots, redaction fixtures, and exported code union; FW-701 and FW-703 remain independent gates.
  - Verification: `pnpm run format` exit 0; `pnpm run verify` exit 0 — format and lint completed over 42 files, strict TypeScript build passed, 9 test files and 167 tests passed, no known vulnerabilities, and the license gate passed for 68 packages across 6 license expressions. Biome emitted one pre-existing warning and four informational suggestions outside FW-006; none are gate failures.
  - Security impact: high taxonomy relevance, no implemented security boundary. Static codes and slugs exclude dynamic or attacker-controlled values; `NUSA-SECURITY-*` is restricted to `error` or `warning` and documented as non-suppressible; third-party strings cannot claim core identity. A code does not make accompanying metadata safe. This task implements no formatter, production serializer, redaction control, or runtime enforcement and discharges no `SEC-*` requirement.
  - Residual: the registry JSON Schema is reviewable structure but is not yet wired to a general schema engine; the focused test provides current mechanical enforcement. No official documentation origin or error pages exist, so the registry stores deterministic slugs only. Production-safe classification and leakage tests do not exist. These are explicit FW-102 obligations, not missing FW-006 claims.
- [x] **FW-007 [DONE/P1] Create the benchmark harness baseline** — depends FW-002 (DONE); promoted to `READY` 2026-08-17 under delegated autonomy, started and completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - AC (derived from `docs/10_PERFORMANCE_AND_BENCHMARKS.md`): AC-1 every implemented fixture runs through one documented root command; AC-2 raw samples and safe environment metadata are retained; AC-3 short-latency runs require at least 30 samples and report median, p95, p99, minimum, maximum, mean, and population standard deviation; AC-4 correctness and security prerequisites complete before warm-up or measurement; AC-5 all B01–B11 fixtures are registered without claiming unimplemented product evidence; AC-6 calibration output is explicitly excluded from framework and marketing claims.
  - Evidence: `benchmarks/harness.mjs`, `benchmarks/fixtures/harness-calibration.mjs`, `benchmarks/fixtures.json`, `benchmarks/README.md`, `benchmarks/BENCHMARK_MATRIX.md`, and `tests/benchmark-harness.test.ts` (12 tests). `package.json` exposes `benchmark` and `benchmark:list`; generated results are excluded in `.gitignore`.
  - AC result: AC-1 PASS through `pnpm run benchmark`; AC-2 PASS with schema version, commit, runtime, OS, architecture, CPU, memory, configuration hash, methodology, prerequisites, every sample, and summary; AC-3 PASS and fewer than 30 samples are rejected; AC-4 PASS because prerequisite failure aborts before warm-up and writes no result; AC-5 PASS with B01–B11 marked `planned` and only `harness-calibration` marked `implemented`; AC-6 PASS through the result's `claimScope` and methodology documentation.
  - Architecture: no ADR required. The harness is repository-internal evidence tooling, implements methodology already fixed by the performance PRD, and adds no framework public API, package, runtime behavior, or architecture boundary.
  - Verification: `pnpm run format` exit 0; `pnpm run verify` exit 0 — 39 files formatted and linted, strict TypeScript build passed, 8 test files and 159 tests passed, no known vulnerabilities, license gate passed for 68 packages across 6 license expressions. `pnpm run benchmark` exit 0 and wrote the ignored calibration artifact; `pnpm run benchmark:list` listed B01–B11 plus calibration; `graft build` refreshed 148 nodes, 283 edges, and 31 cards.
  - Security impact: medium for tooling, none for framework runtime. The harness imports only lowercase registered fixture slugs from `benchmarks/fixtures`, confines output to the repository, avoids environment-variable and absolute-path capture, invokes Git without a shell or attacker-controlled arguments, and refuses timing unless explicit correctness and security statuses are `pass`. Fixture code is trusted repository code; prerequisite status is scoped evidence and discharges no `SEC-*` requirement.
  - Residual: the only executable fixture is deterministic harness calibration and is not a framework benchmark or approved baseline. B01–B11, pinned comparison frameworks, browser capture for zero JS, CI artifact retention, and regression detection against an approved baseline remain future work under their owning implementation and performance tasks. Timing was observed only on local Windows/Node 24 and supports no cross-platform claim.
- [x] **FW-008 [DONE/P0] Approve the threat model and security PRD** — depends FW-001 (DONE); promoted to `READY` 2026-08-15 under delegated autonomy, started and completed 2026-08-15, agent: GitHub Copilot (`gpt-pro`)
  - AC (derived; the checklist line carries no AC sub-bullet, so acceptance is taken from the governing documents): AC-1 the threat model in `docs/09_SECURITY_PRD.md` sections 4, 5, 6, and 22 is reviewed against the architecture in `docs/02_ARCHITECTURE.md` and every gap is either closed or recorded as an owned question (`docs/12_ROADMAP_AND_RELEASES.md` M0 exit: "no unowned P0 question"); AC-2 every P0 `SEC-*` requirement maps to a named owning task and a planned evidence type (`docs/09_SECURITY_PRD.md` section 7, AC-SEC-01); AC-3 every P1 `SEC-*` requirement has a named owner and milestone deadline or is recorded as an unowned gap requiring a checklist amendment; AC-4 the approval record is linked from `docs/14_REQUIREMENTS_TRACEABILITY.md` as the FR-013 and AC-SEC-01–10 evidence artifact; AC-5 the record states what approval does not mean, so it cannot be mistaken for the section 26 v1-RC independent review.
  - AC-1 PASS: `docs/SECURITY_THREAT_MODEL_APPROVAL.md` sections 2 and 6. All eight architecture layers map to at least one section 6 trust boundary; all eleven protected assets and all nine threat actors map to at least one requirement family; thirteen of the fourteen abuse cases in section 22 map to an owning task and the fourteenth (a fetch plugin redirected into a private network) is recorded as finding F-5 with owners FW-701 and FW-603. Eleven findings F-1 through F-11 are recorded, each with a named owner, so no question is unowned.
  - AC-2 PASS: coverage table in section 4 maps all 38 `P0` requirements. Thirty-seven name an implementing task; `SEC-SSRF-001` is discharged by a reproducible manual gate (the FW-701 public API audit confirming core ships no outbound-fetch helper) with an ADR precondition before one may be added. Enforced by `tests/security-coverage.test.ts` ("gives every P0 requirement an owning task or an explicit gate owner").
  - AC-3 PASS: of 25 `P1` requirements, 21 name an implementing task with a completion milestone; `SEC-SSRF-002`, `SEC-SSRF-003`, and `SEC-FILE-004` are conditional on integrations that do not exist yet (findings F-5, F-7, F-11); `SEC-AUTH-003` is recorded as genuinely unowned in finding F-6, requiring a checklist amendment creating a cookie-primitives task before M2 closes. No unowned requirement was silently assigned an owner.
  - AC-4 PASS: `docs/14_REQUIREMENTS_TRACEABILITY.md` now links the record from the FR-013 row, the AC-SEC-01–10 row, and a preamble above the security-traceability table stating that the family rows are summaries and the requirement-level map lives in the approval record. Enforced by the test "is linked from the traceability matrix as security evidence".
  - AC-5 PASS: section 1.1 states five explicit exclusions — the record is not the section 26 v1-RC independent review (owned by FW-703), asserts no implementation, and does not absorb FW-009, FW-018, or FW-019 scope. Enforced by the test "states the limits of the approval so it cannot be read as a release gate".
  - Evidence: `docs/SECURITY_THREAT_MODEL_APPROVAL.md` (approval decision, architecture cross-check, 63-row coverage map, gate status, eleven findings, residual risk); `tests/security-coverage.test.ts` (13 tests parsing the PRD, the record, and this checklist so the baseline cannot drift — asserts requirement counts match the prose, coverage is exactly the declared set with no stale or duplicate rows, priorities are repeated faithfully, evidence types are limited to the three permitted by section 7 plus `conditional`, every referenced `FW-` ID exists here, every P0 has an owner, and every unowned or conditional row cites a recorded finding).
  - Verification: `pnpm run verify` → `VERIFY_EXIT=0` (`format:check` and `lint` "Checked 19 files. No fixes applied."; `typecheck` silent; Vitest `Test Files 2 passed (2)`, `Tests 24 passed (24)`). `get_errors` on the three changed files → no errors.
  - Security impact: none directly — no code path, dependency, or default changed. Indirect impact is high and positive: 38 P0 requirements now have a named owner and evidence type, and eleven previously implicit gaps are recorded rather than assumed. Two findings would have become silent coverage holes: `SEC-AUTH-003` has no owning task at all, and the `SECURITY.md` artifact required by AC-SEC-10 is named by no task.
  - Residual: the approval is maintainer-level only; the section 26 v1-RC clause "threat model reviewed by maintainers and an independent reviewer" is unsatisfied and owned by FW-703. No security control is implemented or tested, so every coverage row is an obligation and not evidence. Per-PR gates in section 26 are not yet enforceable — CI has no security tests, dependency scanning, secret scanning, or boundary scan (owners FW-019, FW-117, FW-009). The coverage map's owner assignments are proposals binding on tasks not yet `READY`, so FW-114, FW-115, FW-206, and FW-111 must absorb findings F-8, F-9, and F-1 into their acceptance criteria when they start. Version control is still not initialized, so `SEC-SUPPLY-002` and `SEC-SUPPLY-003` remain unexercised and CI has never run.
- [x] **FW-009 [DONE/P0] Define security evidence and accepted-risk workflow** — depends FW-008 (DONE); promoted to `READY` 2026-08-16 under delegated autonomy, started and completed 2026-08-16, agent: GitHub Copilot (`gpt-pro`)
  - AC (derived; the checklist line carries no AC sub-bullet, so acceptance is taken from the governing documents): AC-1 the evidence index format is defined, including what qualifies as evidence for each of the three types permitted by `docs/09_SECURITY_PRD.md` section 7 and how evidence is invalidated when a requirement or owner changes; AC-2 the accepted-risk record format satisfies every field named by the `docs/GLOSSARY.md` definition and the section 26 gate "all P1 requirements are complete or have explicit accepted risk with owner and deadline", and a reusable template exists; AC-3 approval authority, expiry, and renewal are defined, including the limits of single-agent approval; AC-4 every finding recorded by FW-008 has a disposition state and, where the disposition was assigned to FW-009, a resolution; AC-5 the workflow is bound to the four release-gate tiers in section 26 and to the pull-request gates in `docs/11_TESTING_AND_QUALITY.md`; AC-6 the workflow is enforced by automated checks wherever the constraint is mechanical, and the non-mechanical parts are named as manual gates.
  - AC-1 PASS: `docs/SECURITY_EVIDENCE_WORKFLOW.md` sections 3.1 to 3.3 define qualification and non-qualification criteria for all three permitted evidence types, the three evidence homes, and six invalidation triggers. AC-2 PASS: section 6.2 defines ten mandatory fields covering every field named by `docs/GLOSSARY.md` plus the section 7 deadline; `templates/ACCEPTED_RISK_TEMPLATE.md` is the reusable form and `docs/SECURITY_ACCEPTED_RISKS.md` the register. AC-3 PASS: sections 6.3 and 6.4 define the approval-authority matrix, the prohibition on an agent promoting a `P0` record, the six-state lifecycle, `EXPIRED` as a release blocker, and a two-renewal cap. AC-4 PASS: section 8 gives all eleven FW-008 findings a disposition; F-4, F-5, F-7, F-11 `RESOLVED`, F-6 and F-10 `AMENDED` by creating FW-218 and FW-709, F-1, F-2, F-3, F-8, F-9 `SCHEDULED` to existing owners. AC-5 PASS: section 7 binds six pull-request gate conditions to their mechanism owners, defines the prerelease milestone check, and specifies the eight-section release-candidate evidence index. AC-6 PASS: `tests/security-evidence-workflow.test.ts` adds 15 tests enforcing the mechanical constraints, and section 10 names the four judgements that remain manual gates.
  - Evidence: `docs/SECURITY_EVIDENCE_WORKFLOW.md` (11 sections); `docs/SECURITY_ACCEPTED_RISKS.md` with `AR-001` as `PROPOSED` covering `SEC-SUPPLY-002` and `SEC-SUPPLY-003`; `templates/ACCEPTED_RISK_TEMPLATE.md`; `tests/security-evidence-workflow.test.ts`; amendment A-1 appended to `docs/SECURITY_THREAT_MODEL_APPROVAL.md` with the `SEC-AUTH-003`, `SEC-AUTH-004`, and `SEC-SUPPLY-002/003` rows updated; FW-218 and FW-709 added to this checklist with acceptance criteria and FW-707's dependency list extended; `docs/14_REQUIREMENTS_TRACEABILITY.md` FR-013, AC-SEC-01–10, and the evidence-index rule updated to cite the new documents.
  - Verification: `pnpm run format` → `FORMAT_EXIT=0`; `pnpm run verify` → `VERIFY_EXIT=0`, "Checked 20 files. No fixes applied." for both `format:check` and `lint`, `tsc --build` silent, `Test Files 3 passed (3)`, `Tests 39 passed (39)` (11 workspace skeleton, 13 security coverage, 15 evidence workflow). Parser non-vacuity confirmed independently: 4 conditional rows, 11 dispositions, 11 findings, 1 risk record.
  - Security impact: medium. No control is implemented; the change defines how security evidence is qualified, recorded, and deferred, and closes the two unowned-requirement findings that FW-008 escalated. `SEC-AUTH-003` gains an implementing owner, so no `P1` requirement is unowned. `SEC-SUPPLY-002` and `SEC-SUPPLY-003` remain undischarged but are now a tracked, bounded record rather than repeated prose.
  - Residual: interpretation I-1 restricting `P0` accepted risks is unconfirmed by a human maintainer; no pull-request security gate is enforceable until FW-019, FW-117, and FW-120 land; `AR-001` is `PROPOSED` and cannot be approved by this agent; the prerelease milestone check of section 7.2 is manual until a milestone closes.
- [x] **FW-018 [DONE/P0] Define strict security mode and fail-closed policy** — depends FW-008 (DONE); promoted to `READY` 2026-08-16 under delegated autonomy, started and completed 2026-08-16, agent: GitHub Copilot (`gpt-pro`)
  - AC (derived; the task line states an outcome, not acceptance criteria): AC-1 fail-closed behavior is defined as a rule set rather than a slogan, and every rule has a stated required outcome; AC-2 every relaxation of a security control that `docs/09_SECURITY_PRD.md` permits is inventoried with its opt-in form, its required diagnostic, and its owning task; AC-3 the severity and suppression rules for security diagnostics are fixed; AC-4 the strict-mode configuration surface has an architecture decision record; AC-5 the definition is propagated to the requirement index, the glossary, and the approved threat-model baseline; AC-6 the mechanical parts are enforced by an automated test.
  - ADR: `docs/adr/ADR-008-security-manifest-and-strict-mode.md` created as `Proposed`, deliberately split in two. Part 1, strict-mode behavior, proposes Option C — `security.mode` exists with `"strict"` as its only legal value in v0.x — and is what this task needed. Part 2, the security-manifest schema, is left undecided and assigned to FW-107. The record is **not accepted**, so its Decision section may not be implemented against; `docs/13_POSITIONING_RISKS_AND_DECISIONS.md` now cites the file and its status. Every clause of the policy that depends on the mode is marked conditional on ADR-008, which is why the task completed rather than blocking: the fail-closed rules and the escape-hatch inventory are requirement interpretation that holds whether or not the mode key survives, and no implementation is unblocked because the first task that reads configuration is FW-103.
  - AC-1 PASS: `docs/STRICT_SECURITY_MODE.md` section 4 states rules FC-1 to FC-10 — refusal not repair, absence is the strict value, an unknown value is an error not a default, an unsupported capability fails the build, an unsupported schema version fails closed, a control that cannot run refuses the operation, an error inside a control is a closed outcome, a limit is exceeded not stretched, ambiguity is refused, degradation is refusal not reduction — and section 4.1 maps thirteen concrete conditions to a required outcome and the rule that supplies it.
  - AC-2 PASS: section 5 inventories fourteen relaxations covering `SEC-INPUT-002`, `SEC-XSS-001`, `SEC-REQ-002`, `SEC-REQ-003`, `SEC-REQ-004`, `SEC-AUTH-003`, `SEC-CACHE-001`, `SEC-SECRET-002`, `SEC-SECRET-004`, `SEC-DEV-001`, `SEC-SUPPLY-005`, `SEC-SUPPLY-006`, `SEC-SSRF-003`, and `SEC-OBS-004`, each with the required opt-in form, the required diagnostic or manifest entry, and the owning task. Eleven were derived mechanically from the security PRD's own exception clauses; three — `SEC-INPUT-002`, `SEC-XSS-001`, `SEC-SUPPLY-005` — are added by judgement and disclosed as interpretation I-2. All three properties an escape hatch must have, local, named, and visible, are stated.
  - AC-3 PASS: section 6 fixes `error` when a control did not or cannot run, `warning` for a correctly declared relaxation, `warning` for context-dependent risk, and no `info` severity for any security diagnostic; it then prohibits every suppression mechanism — ignore comment, severity override, code allowlist, global switch, plugin, configuration — and binds the prohibition to the section 26 prerelease gate that a suppression mechanism would render unenforceable while appearing to pass.
  - AC-4 PASS: ADR-008 exists with three compared options, a security analysis naming the affected trust boundaries and the misconfigured-operator abuse case, and a rollback plan. Status `Proposed`, acceptance reserved to the core maintainers; section 9's judgement table records that no agent may accept it.
  - AC-5 PASS: `docs/14_REQUIREMENTS_TRACEABILITY.md` NFR-011 no longer says only "Invalid-config security suite" and now names the rule set, the inventory, the suppression prohibition, and the enforcing test; the FR-013 and AC-SEC-01–10 evidence cells name the policy alongside the other three security documents. `docs/GLOSSARY.md` gains **Escape hatch**, **Fail closed**, and **Strict security mode**. `docs/SECURITY_THREAT_MODEL_APPROVAL.md` gains amendment A-3, which records the constraint and explicitly transfers no ownership and adds no coverage row, because a policy that reassigned fourteen owners to itself would make the coverage map claim FW-018 tests controls it does not implement.
  - AC-6 PASS: `tests/strict-security-mode.test.ts`, 33 tests. It proves the FC rule set is contiguous and that the rules and the decision table reach each other in both directions; that every inventoried requirement exists at the priority quoted **and that its own normative text contains an exception-permitting clause**, so no escape hatch is fabricated; that every owner and every `FW-` reference resolves in this checklist; that suppression is absent from the two documents that would have to define it; that FW-018 appears in no coverage row as an owner; that ADR-008 has a permitted status, follows the template, compares at least three options, and is reachable from the reserved-decision list; and that interpretation numbers do not collide with the evidence workflow's I-1.
  - Evidence: `docs/STRICT_SECURITY_MODE.md` (eleven sections), `docs/adr/ADR-008-security-manifest-and-strict-mode.md` (`Proposed`), `tests/strict-security-mode.test.ts`, amendment A-3, three traceability cells, three glossary entries, one reserved-decision line.
  - Verification: `pnpm run verify` exit 0 — `Checked 23 files`, `Test Files 5 passed (5)`, `Tests 89 passed (89)` in 704 ms, `No known vulnerabilities found`, `License gate passed: 68 packages across 6 license expressions`. The one lint warning and two infos are pre-existing in `scripts/check-licenses.mjs` and `tests/supply-chain-policy.test.ts`; the new test file reports no diagnostics. One real defect was caught by the new test and fixed: the policy attributed the three-severity diagnostic shape to `docs/08_DX_AND_OBSERVABILITY.md` when `docs/06_COMPILER_AND_DEV_SERVER.md` defines it.
  - Security impact: medium. No security control is implemented and, per `docs/SECURITY_EVIDENCE_WORKFLOW.md` section 3.2, **no `SEC-*` requirement is discharged** — the task's evidence block names none. What changes is that NFR-011, SO-04, and SO-05 now have a definition the fourteen tasks listed against NFR-011 must satisfy instead of each deciding fail-closed behavior separately, which is how risk R-12 is realized. Nothing is weakened, no priority is lowered, no owner is moved.
  - Residual: ADR-008 is `Proposed`, so every mode-dependent clause is conditional and FW-103 must not start until it is accepted; part 2 is undecided, so every "recorded in the security manifest" cell has no destination until FW-107; nothing in section 4 is enforced by code, only by review; interpretations I-2, I-3, and I-4 are unconfirmed by a human maintainer, and I-4 is deliberately stricter than `docs/02_ARCHITECTURE.md` for `emulated` capabilities; section 6's prohibition offers no pressure valve and is the clause most likely to be amended under pressure rather than on merit; no gate has executed because version control is still uninitialized, and this task deliberately does not renew `AR-001`, which has one renewal left before mandatory escalation.
- [x] **FW-019 [DONE/P0] Define dependency, license, provenance, and publishing policy** — depends FW-002 (DONE), FW-008 (DONE); promoted to `READY` 2026-08-16 under delegated autonomy, started and completed 2026-08-16, agent: GitHub Copilot (`gpt-pro`)
  - AC (derived; the checklist line carries no acceptance criteria, so these are derived from `docs/09_SECURITY_PRD.md` section 15, section 23, section 26, and `AC-SEC-09`; `docs/11_TESTING_AND_QUALITY.md` pull-request gate 8; `docs/12_ROADMAP_AND_RELEASES.md` release channels and governance; and FW-008 finding F-2): AC-1 a dependency-addition policy requires and records maintenance, ownership, install-script, transitive-risk, and license review, with install scripts requiring explicit approval (`SEC-SUPPLY-005`). AC-2 locked dependencies plus vulnerability, secret, and license scanning are specified and wired into continuous integration (`SEC-SUPPLY-002`, pull-request gate 8, and the section 26 conditions "No new critical dependency finding" and "No secret-scanning finding"), with a stated definition of a critical finding and a stated failure action. AC-3 release provenance and controlled-CI publishing with least privilege and protected environments are specified, including the publish dry run as a named reproducible manual gate (`SEC-SUPPLY-003`). AC-4 the release-artifact-to-consuming-application trust boundary absent from `docs/09_SECURITY_PRD.md` section 6 is recorded, closing FW-008 finding F-2. AC-5 `AR-001` is disposed of per `docs/SECURITY_EVIDENCE_WORKFLOW.md` section 6.4 before this task is `DONE`, as its `Review by` predicate requires. AC-6 the policy is enforced by automated checks wherever mechanical, and every non-mechanical part is named as a manual gate with its executing role.
  - No ADR fires. FW-019 introduces no public API and selects no architecture: it defines policy over the toolchain that ADR-005 already accepted, and ADR-005 explicitly defers dependency-addition policy to this task ("Dependency additions remain subject to the policy work in FW-019"). No reserved ID in `docs/13_POSITIONING_RISKS_AND_DECISIONS.md` covers dependency, license, provenance, or publishing policy; ADR-006 covers the supported runtime and TypeScript policy and belongs to FW-005. This follows the FW-008 and FW-009 precedent, both of which were governance tasks that correctly proceeded without an ADR.
  - AC-1 PASS: `docs/SUPPLY_CHAIN_POLICY.md` section 4 defines the five-dimension review table (maintenance, ownership, install and download script, transitive risk, license) with what is recorded and the rejection condition for each, and assigns the review to the pull-request approver rather than the author; section 4.1 makes install-script approval deny-by-default and names the workspace entry and the test that fails if one is granted silently. Enforced by "records all five dependency review dimensions" and "requires explicit approval before any install script runs".
  - AC-2 PASS: section 5 is the license allow list, section 6.1 names the three scanners, section 6.2 defines a critical dependency finding and what makes one new (absent from the merge-base scan), and section 6.3 states the four-step failure action while prohibiting suppression and lowering `--audit-level`. `pnpm run deps:audit` and `pnpm run deps:licenses` exist as scripts, are steps in `.github/workflows/ci.yml`, and are inside `pnpm run verify`. Enforced by "defines a critical dependency finding and what makes it new", "states a failure action that does not weaken the gate", and "wires every named scanner into both the manifest and continuous integration".
  - AC-3 PASS: section 8 specifies publishing only from controlled CI on a tag, a protected environment with required reviewers, `contents: read` plus `id-token: write`, `pnpm publish --provenance` over OIDC, an explicit `files` allow list, and the three release channels; section 8.1 is the publish dry run as a manual gate with an executing role that is not the release author, eight numbered steps, an expected observation, an artifact, and a failure action. Enforced by "specifies the publish dry run as a complete reproducible manual gate".
  - AC-4 PASS: a tenth row, "Release artifact to consuming application", is appended to the `docs/09_SECURITY_PRD.md` section 6 boundary table with its four controls, and section 9 of the policy explains the asymmetry. F-2 is marked resolved in both `docs/SECURITY_THREAT_MODEL_APPROVAL.md` and section 8 of `docs/SECURITY_EVIDENCE_WORKFLOW.md`, and amendment A-2 records the change. Enforced by "records the release-artifact trust boundary in every document that carries it" and "closes FW-008 finding F-2 through a recorded amendment".
  - AC-5 PASS: `AR-001` is `RENEWED`, not approved — section 6.3 forbids this agent from promoting a `P0` record. `Review by` moved from the now-satisfied `before FW-019 is marked DONE` predicate to `M0`, a `Renewed` field records that this is the first of the two permitted renewals, and the scope, rationale, compensating controls, and remediation are restated to describe the narrower residual gap. Enforced by "disposes of the accepted risk whose review-by date this task reached".
  - AC-6 PASS: `tests/supply-chain-policy.test.ts` adds 17 tests, and `scripts/check-licenses.mjs` reads the allow list out of section 5 so the policy and its enforcement cannot drift. Section 11 names the six judgements that stay manual with their executing roles.
  - Evidence: `docs/SUPPLY_CHAIN_POLICY.md` (13 sections, including section 12's record of the first execution of section 6.2 and section 13's seven residual-risk bullets); `scripts/check-licenses.mjs`; `package.json` scripts `deps:audit`, `deps:licenses`, `deps:check`, with `verify` extended; two scan steps in `.github/workflows/ci.yml`; the tenth trust-boundary row in `docs/09_SECURITY_PRD.md`; amendment A-2 plus the `SEC-SUPPLY-002`, `SEC-SUPPLY-003`, and `SEC-SUPPLY-005` coverage rows in `docs/SECURITY_THREAT_MODEL_APPROVAL.md`; the `AR-001` renewal in `docs/SECURITY_ACCEPTED_RISKS.md`; the F-2 disposition, section 7.1 gate status, and section 11 residual bullets in `docs/SECURITY_EVIDENCE_WORKFLOW.md`; `docs/14_REQUIREMENTS_TRACEABILITY.md` rows FR-013, NFR-006, `SEC-SUPPLY-*`, AC-PLUGIN-01–05, and AC-SEC-01–10.
  - Verification: `pnpm run format` → exit 0, "Formatted 22 files in 16ms. Fixed 2 files."; `pnpm run verify` → `VERIFY_EXIT=0` with `Test Files 4 passed (4)`, `Tests 56 passed (56)` (11 workspace skeleton, 13 security coverage, 15 evidence workflow, 17 supply-chain policy), `pnpm audit --audit-level high` → "No known vulnerabilities found", `node scripts/check-licenses.mjs` → "License gate passed: 68 packages across 6 license expressions, all allowed by docs/SUPPLY_CHAIN_POLICY.md section 5." `get_errors` on every changed file → no errors.
  - Security impact: medium and positive. `SEC-SUPPLY-005` is discharged as policy plus the deny-by-default enforcement test. `SEC-SUPPLY-002` is discharged for its dependency and license halves as locally executing gates and specified CI gates; its secret-scanning half stays owned by FW-120. `SEC-SUPPLY-003` is specified, including the manual dry run, but cannot be exercised before a real release candidate. `AC-SEC-09` is discharged as to the policy and workflow definition only. The section 6.2 scanner found a real critical advisory on its first run — `vitest@3.2.4`, advisory 1139528 — which was remediated to 3.2.7 rather than suppressed; that is recorded in section 12. No default was weakened and no requirement, priority, or evidence type was downgraded.
  - Residual: no gate in `.github/workflows/ci.yml` has ever executed, because version control is not initialized — this is why `AR-001` was renewed instead of retired, and an unexecuted gate is not evidence under `docs/SECURITY_EVIDENCE_WORKFLOW.md` section 3.1. Secret scanning is specified but unenforced until FW-120. The license checker reads declared license fields only and cannot detect a misdeclared license. `pnpm audit` reflects published advisories, so a zero result is a statement about the registry and not about the code. The merge-base comparison in section 6.2 is unimplementable without git, so the gate currently blocks on any high or critical advisory, which is stricter than required. Nothing has been published, so provenance is entirely unexercised. The six devDependencies in `spikes/renderer-evaluation/` predate this policy and were never reviewed under section 4.

## M1 — Kernel

- [x] **FW-101 [DONE/P0] Core package and public API boundary** — depends FW-002 (DONE), FW-005 (DONE); completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Outcome: added the private ESM-only, dependency-free universal `@nusajs/core` package with one explicit stable root entry point and no facade, wildcard, public internal path, or empty experimental entry point.
  - Acceptance criteria: runtime identity exports PASS; strict type usage PASS; negative deep-import fixtures PASS; public declaration snapshot PASS; source and built-output Node/runtime dependency scans PASS; package documentation/example PASS.
  - Evidence: `pnpm run verify` PASS (format, lint, build, root/core type checks, 6 package tests, high-severity audit, and license policy); focused `pnpm exec vitest run packages/core/tests` PASS; `pnpm --filter @nusajs/core typecheck` PASS.
  - Public API changed: yes — introduced `CORE_PACKAGE_NAME`, `CORE_VERSION`, `CorePackageName`, and `CoreVersion` at the accepted private package root.
  - Security impact: low — establishes a closed export boundary and verifies that source/build output has no Node built-ins or runtime dependencies; no parser, serializer, network, filesystem runtime, cache, plugin hook, or request trust boundary was added.
  - Documentation/ADR: package README and API report added; implementation follows accepted ADR-018.
  - Remaining risks: package is intentionally private and identity-only until later framework contracts are accepted; publishing remains unauthorized.
  - Blocker: this task creates the first framework package and candidate public entry points. `AGENTS.md` requires an accepted ADR before a new public API or architecture change. ADR-018 (`docs/adr/ADR-018-core-package-and-public-entry-points.md`) is drafted as `Proposed`; only core maintainers may accept the package topology, direct-core-import policy, explicit export map, experimental entry point, and absence of an initial facade.
  - Proposed outcome: create private ESM-only `@nusajs/core`, expose only explicit stable and experimental entry points, forbid wildcard and public `internal/*` exports, keep core free of Node.js and workspace runtime dependencies, and defer diagnostics, configuration, routing, request context, renderer, and cryptographic contracts to their owning tasks.
  - Acceptance needed: core maintainers must approve direct imports from `@nusajs/core`, decide whether `@nusajs/core/experimental` should exist while empty, confirm that no `nusajs` facade is created in M1, and require the eight implementation-evidence items in ADR-018 before changing its status to `Accepted`.
  - Security impact: medium. Package exports cross application, plugin, package, and supply-chain boundaries. The proposal denies deep imports, Node.js built-ins, runtime dependencies, mutable request globals, environment/filesystem exposure, custom cryptography, and publication under the provisional unreserved scope. It implements no control and discharges no `SEC-*` requirement.
  - Verification: implementation, package creation, API report, consumer and negative import fixtures, type/runtime tests, built-output scan, documentation example, and public API reporting are deliberately not started. Existing repository verification remained green before task selection; the proposal itself must now pass formatting and repository document checks.
- [x] **FW-102 [DONE/P0] Diagnostic model and formatters** — depends FW-006 (DONE), FW-101 (DONE); completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Outcome: implemented the universal immutable diagnostic model, registry-derived active/tombstone code types, deterministic development text/JSON formatters, HTTPS documentation URLs, and fail-closed production serialization.
  - Acceptance criteria: structured model and ordered source ranges PASS; active/retired and security-severity enforcement PASS; hostile-value production redaction PASS; safe request-ID validation PASS; deterministic snapshots PASS; TSDoc, type tests, 20 diagnostic runtime/security tests, package scans, example, and transitive API report PASS.
  - Evidence: `pnpm run verify` PASS; 3 package test files and 26 tests PASS; `pnpm --filter @nusajs/core typecheck` PASS; high-severity audit reports no known vulnerabilities; license gate PASS.
  - Public API changed: yes — added diagnostic code/severity/range/input/output types and four creation/formatting functions at the existing root entry point.
  - Security impact: medium — diagnostic values can contain attacker-controlled or sensitive data; production output must expose exactly an allowlisted static code and validated request ID.
  - Documentation/ADR: governed by accepted ADR-007; proposed ADR-019 records the concrete public API and trust-boundary behavior.
  - Remaining risks: ADR-019 remains proposed and must be accepted before stable publication; development formatters intentionally preserve diagnostic values and therefore remain development-only; the initial production-safe allowlist contains only the generic server code.
- [x] **FW-103 [DONE/P0] Config loader and schema validation** — depends FW-101 (DONE), FW-102 (DONE); ADR-008 part 1 accepted by maintainer direction 2026-08-17; completed 2026-08-17, agent: Freebuff (`buffy`)
  - Outcome: implemented the established safe scope in `@nusajs/compiler` — `parseConfig()` statically loads and validates framework configuration through the TypeScript parser without executing it; literal values are extracted, function calls such as `adapter: node()` are recorded as dynamic values (never executed, never guessed); the schema enforces `output: "server" | "static"` and ADR-008 part 1 (`security.mode` legal value `"strict"` only, absent means strict, any other value including case variants fails the build); every failure emits `NUSA-CONFIG-0001` with the exact property path, expected value, secret-free received description, remediation, and docs slug; reserved names (`__proto__`/`constructor`/`prototype`) are rejected before assignment, nesting is depth-bounded, and unknown properties fail closed.
  - Acceptance criteria: valid static config loads without execution PASS; absent keys default (`strict`/`server`) PASS; illegal mode, case variants, invalid output, unknown top-level/security properties, non-object security, syntax errors, and missing config fail closed with exact paths PASS; received descriptions never include values (secret-free, asserted by JSON scan) PASS; reserved-name and depth defenses PASS; dynamic values recorded with visible paths PASS; deterministic repeated parses PASS; package boundary updated for the pinned TypeScript parser dependency PASS.
  - Verification: `pnpm run verify` PASS — format/lint clean over 87 files; strict build/typecheck PASS; universal boundary gate PASS (compiler stays Node-only); 20 test files/233 tests PASS (13 new config tests); no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions.
  - Public API changed: yes — `parseConfig()` and config types on the private compiler boundary; one pinned dependency (`typescript@5.9.3`, already the repository toolchain, SEC-SUPPLY-005 reviewed).
  - Security impact: high and positive — eliminates arbitrary config execution, secret disclosure, prototype pollution, and silent security downgrade in configuration loading; deferred security-manifest schema remains FW-107.
- [x] **FW-104 [DONE/P0] Filesystem route scanner** — depends FW-004 (DONE), FW-101 (DONE); completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Outcome: added the private Node-only `@nusajs/compiler` boundary and deterministic, non-executing suffix-convention route discovery below an explicit absolute root.
  - Acceptance criteria: page/endpoint/layout/error/loading classification PASS; static, dynamic, optional, catch-all, optional catch-all, and transparent-group path fixtures PASS; unrelated-file exclusion PASS; immutable deterministic output PASS; absolute/readable root validation PASS; realpath containment and escaping-symlink rejection PASS; reserved Windows device-name rejection PASS; NFC/casefold collision detection PASS when the host filesystem can represent both spellings; package/type evidence PASS.
  - Evidence: `pnpm run verify` PASS; 5 package test files and 36 tests PASS; focused compiler build/typecheck and 10 tests PASS; audit reports no known vulnerabilities; license gate PASS.
  - Public API changed: no — `@nusajs/compiler` is private and scanner contracts remain internal pending ADR acceptance.
  - Security impact: medium — filesystem traversal is a Node trust boundary; scanner canonicalizes every entry, confines real paths to the configured root, rejects escaping symlinks and portability hazards, and never imports route modules.
  - Documentation/ADR: accepted ADR-003 and ADR-004 govern behavior; proposed ADR-020 records the private compiler package boundary; package README documents the internal contract.
  - Remaining risks: full URL-pattern parsing, group erasure, flat/index alias collision, and precedence belong to FW-105; full filesystem conformance belongs to FW-119; case/NFC duplicate evidence is necessarily conditional on whether the host filesystem can represent both names; ADR-020 remains Proposed.
- [x] **FW-105 [DONE/P0] Route parser and collision detection** — depends FW-104 (DONE); completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Outcome: added a pure internal parser that converts normalized scanner records into immutable route/boundary graphs, validates the accepted segment grammar, erases transparent groups, canonicalizes flat/index spellings, and computes deterministic specificity.
  - Acceptance criteria: all static/dynamic/optional/catch-all/optional-catch-all/group/page/endpoint/layout/error/loading fixtures PASS; invalid parameter grammar and non-final catch-all rejection PASS; page/endpoint role separation PASS; flat/index, renamed-parameter, group-transparency, optional/static, and optional-catch-all/index collision fixtures report every conflicting file PASS; record-order independence PASS; type evidence PASS.
  - Evidence: `pnpm run verify` PASS; 6 package test files and 49 tests PASS; strict declaration/typecheck PASS; audit reports no known vulnerabilities; license gate PASS.
  - Public API changed: no — parser contracts remain internal to private `@nusajs/compiler`.
  - Security impact: low — pure untrusted-path parsing is fail-closed, bounded by input route count, never executes application modules, and emits stable route diagnostics without source contents.
  - Documentation/ADR: accepted ADR-003 governs filesystem grammar and collision semantics; accepted ADR-004 governs non-execution; package README documents the internal parser stage.
  - Remaining risks: matcher correctness and malicious URL normalization remain FW-106; manifests remain FW-107; typed parameter generation remains FW-108; 10,000-route performance budget remains later benchmark work.
- [x] **FW-106 [DONE/P0] Universal route matcher** — depends FW-105 (DONE); started and completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Outcome: added `createRouteMatcher()` to universal `@nusajs/core`; it copies and validates structurally compatible compiler routes, sorts only by parser-derived specificity, keeps page/endpoint roles explicit, and returns frozen null-prototype params and frozen match results.
  - Acceptance criteria: static, dynamic, optional, catch-all, optional-catch-all, root, page/endpoint, specificity, and input-order fixtures PASS; strict UTF-8/NFC decoding and case-sensitive comparison PASS; malformed/incomplete escapes, single/double encoded separators, literal backslashes, duplicate/trailing slashes, literal/encoded dot segments, controls, invalid UTF-8, query/fragment/full-URL input, 8 KiB, and 256-segment bounds fail closed PASS; TSDoc, type tests, README example, and deterministic API report PASS.
  - Evidence: `pnpm run verify` PASS — format/lint clean over 78 files; strict build and all package type tests PASS; universal boundary scan PASS for 2 packages; 12 test files and 110 tests PASS; no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions. Focused core suite: 7 files and 77 tests PASS.
  - Public API changed: yes — added matcher route/segment/result contracts and `createRouteMatcher()` at the existing core root, governed by accepted ADR-021.
  - Security impact: high and positive — one adapter-independent raw-pathname boundary rejects normalization ambiguity without repair; matching uses no regex and optional branching memoizes failed states, bounded by validated route/path limits. Parameters remain explicitly untrusted.
  - Documentation/ADR: ADR-021 accepted with the exact normalization table; core README and API report updated.
  - Remaining risks: AC-ROUTE-07 still requires running the shared corpus against every future adapter under FW-118; AC-ROUTE-06 still requires the approved 10,000-route performance fixture; canonical redirects remain an explicit later pipeline policy.
- [x] **FW-107 [DONE/P0] Route and security manifests** — depends FW-105 (DONE), FW-008 (DONE); ADR-008 part 2 appended and accepted by maintainer direction 2026-08-17; completed 2026-08-17, agent: Freebuff (`buffy`)
  - Outcome: appended and accepted the manifest half of ADR-008 (part 2) recording the versioned schemas, then implemented them in `@nusajs/compiler` — `createRouteManifest()` (deterministic role+pattern-derived route identities, kind/pattern/segments/specificity/file, stable across scan order), `createSecurityManifest()` (mode always `"strict"`, name-only `relaxations`/`publicEnv` identifier lists, `{code,count}` diagnostic summaries, no secret values ever — SEC-SECRET-002/003, AC-ARCH-06), `createCapabilityManifest()` (closed vocabulary from the architecture doc), and `assertManifestSupported()` so every consumer rejects unsupported schema major versions (AC-ARCH-02).
  - Acceptance criteria: versioned route manifest deterministic across order PASS; stable route identities PASS; security manifest records strict posture and names only (values never appear, asserted by JSON) PASS; invalid relaxation/diagnostic/capability input fails closed with NUSA-CONFIG-0001 PASS; capability vocabulary closed to the documented set PASS; version gate accepts at-or-below majors and rejects unsupported majors/wrong schemas/invalid versions PASS; TSDoc, type tests, README PASS.
  - Verification: `pnpm run verify` PASS — format/lint clean over 90 files; strict build/typecheck PASS; universal boundary gate PASS; 21 test files/244 tests PASS (11 new manifest tests); no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions.
  - Public API changed: yes — three manifest generators, their types, and the version gate on the private compiler boundary; governed by Accepted ADR-008.
  - Security impact: high and positive — gives the framework its first versioned security posture artifact (no values), the capability contract for adapter conformance, and a version gate preventing misread of future manifests; FW-108 and FW-120 can now consume stable output.
- [x] **FW-108 [DONE/P0] Typed parameters and href generator** — depends FW-107; completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Implemented deterministic `generateRouteTypes()` in private `@nusajs/compiler`, generating stable `RouteId`, exact immutable `RouteParams`, and generic runtime-safe `href()` from route manifest v1.
  - Acceptance evidence: root/static/dynamic/optional/catch-all/optional-catch-all path generation PASS; scalar and non-empty tuple parameter types PASS; lexical ID ordering and identical output across manifest order PASS; Unicode, space, and embedded slash are encoded per segment PASS; unknown route, missing/extra/empty/wrong-shape parameters and malformed/duplicate manifest routes fail closed with `NUSA-ROUTE-0001` PASS.
  - Public API evidence: Accepted ADR-027, TSDoc, runtime tests, strict compiler type tests, root compiler export, and README documentation.
  - Security impact: medium and positive — validates untrusted manifest shapes and runtime parameter containers and prevents parameter values from injecting pathname separators; generated hrefs remain navigation output, not authorization.
- [x] **FW-109 [DONE/P0] Web-Standard request context** — depends FW-101; completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Implemented public generic `RequestContext` and `createRequestContext()` in universal `@nusajs/core` with Web `Request`, fresh `URL`, frozen null-prototype params, host environment, propagated `AbortSignal`, validated request ID, fresh symbol-keyed locals, and optional `waitUntil`.
  - Isolation/security evidence: 100 concurrent constructions have distinct URL/locals containers; params are copied and frozen; prototype-sensitive names and invalid request IDs fail closed; no Node imports, global state, implicit serialization, or environment cloning.
  - Public API evidence: Proposed ADR-022, TSDoc, runtime tests, type tests, README example, deterministic declaration report, package-root export and deep-import/built-output boundary tests.
  - Verification: `pnpm run verify` PASS — format/lint PASS; strict build/typecheck PASS; 7 test files/57 tests PASS; no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions.
  - Security impact: medium — establishes request-local trust boundary and cancellation propagation; mutable locals cannot cross requests when factory is used, while adapter-owned mutable `env` remains explicitly caller responsibility.
- [x] **FW-110 [DONE/P0] Server request pipeline** — depends FW-106 (DONE), FW-109 (DONE); completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Governing decision: accepted ADR-026 defines one universal `createRequestHandler()` orchestrator with explicit raw pathname input, endpoint-first role selection, GET/HEAD page behavior, pass-through endpoint responses, fresh request context, and exact renderer cleanup rules.
  - Implementation: universal immutable route bindings keyed by route identity; endpoint-first dispatch; fresh matched request context; strict renderer-result validation; buffered/streaming Web `Response` conversion; backpressure-aware stream bridge; exact-once cleanup on completion, cancellation, source error, abort, conversion error, and HEAD body omission; exceptions propagate to adapters.
  - Evidence: public TSDoc/root exports, compiled README example, public type tests, deterministic declaration report, and 16 focused runtime tests covering identity/configuration, dispatch, isolation, pass-through, 404, HEAD, failures, abort, streaming backpressure/cancellation/error/cleanup.
  - Verification: `pnpm run verify` PASS — formatting/lint/strict builds/type tests; universal boundary scan passed for 2 packages; 13 test files/126 tests passed; no known high vulnerabilities; license gate passed for 68 packages across 6 expressions.
  - Security impact: high — this boundary composes attacker-controlled requests with route dispatch, application code, rendering, cancellation, and production error handling.
- [x] **FW-111 [DONE/P0] Renderer contract** — depends FW-003, FW-101; completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Added renderer-neutral universal public types for buffered UTF-8 HTML and Web `ReadableStream<Uint8Array>` delivery, metadata fixed before commit, explicit request context/abort propagation, and mandatory cleanup on success/error/abort paths.
  - Added `defineRenderer()` validation/freeze boundary with stable slug IDs, copied non-empty capabilities, no Node streams, implementation imports, renderer-specific vnode types, raw HTML, serialization, or global nonce API.
  - Public API evidence: Proposed ADR-023, TSDoc, runtime/type tests, README example, deterministic declaration report, and package-boundary coverage.
  - Verification: `pnpm run verify` PASS — format/lint PASS; strict build/typecheck PASS; 8 test files/63 tests PASS; no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions.
  - Security impact: medium — makes cancellation, request-local state, pre-commit metadata, and cleanup explicit; contextual escaping, XSS corpus, backpressure/cancellation behavior, and renderer implementation remain FW-112/conformance responsibilities.
- [x] **FW-112 [DONE/P0] Minimal SSR renderer** — depends FW-111; completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Added public `@nusajs/renderer-preact` package implementing Accepted ADR-002 behind the FW-111 contract with exact-pinned `preact@10.29.8` and `preact-render-to-string@6.7.0` dependencies.
  - Implements deterministic buffered SSR, Web `Headers`, request abort checks before/after rendering, idempotent cleanup, and zero framework bootstrap JavaScript; streaming/hydration/serialization are not falsely claimed.
  - Security evidence: hostile text/attribute/URL-position payload corpus remains escaped; already-aborted work fails before rendering; package output has no Node built-ins; deep imports fail; `dangerouslySetInnerHTML` is documented as an explicit unsafe non-sanitizing escape hatch.
  - Public API evidence: TSDoc, runtime/type tests, README example, deterministic declaration report, exact dependency and package-boundary tests.
  - Verification: `pnpm run verify` PASS — format/lint PASS; strict build/typecheck PASS; 10 test files/73 tests PASS; no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions.
  - Security impact: medium — introduces server-rendered HTML and two reviewed MIT runtime dependencies; serialization, hydration payload validation, full browser XSS/CSP corpus, streaming backpressure, and nonce propagation remain later task responsibilities.
- [x] **FW-113 [DONE/P0] Minimal Node adapter** — depends FW-110 (DONE), FW-112 (DONE); completed 2026-08-17, agent: Freebuff (`buffy`)
  - Outcome: added private `@nusajs/adapter-node` connecting `node:http` to the universal `createRequestHandler()` pipeline: raw origin-form request-target passed unmodified, validated `Host` with explicit `trustProxy` gating of forwarded headers, conservative `maxUrlLength`/`maxRequestSize`/`maxHeaderSize`/`maxHeadersCount` limits (414/413), streaming body and response conversion with backpressure, client-disconnect abort propagation through the request signal, redacted `NUSA-SERVER-0001` production failure responses, and bounded graceful shutdown that force-closes remaining connections.
  - Acceptance criteria: page/endpoint dispatch with dynamic params PASS; endpoint-first role selection PASS; plain-text 404 and page POST rejection PASS; HEAD body omission PASS; raw pathname without decoding/repair (encoded separators 404) PASS; forwarded headers ignored unless `trustProxy` PASS; invalid `Host` 400 PASS; 413/414 limits PASS; ordered streaming chunks PASS; streamed request bodies PASS; client abort reaches renderer cleanup PASS; handler failure returns redacted JSON without error details PASS; bounded shutdown aborts in-flight and refuses new connections PASS; public TSDoc/root exports, README example, type tests, deterministic API report, package-boundary (deep-import rejection, core-only dependency) PASS.
  - Verification: `pnpm run verify` PASS — format/lint clean over 76 files; strict build/typecheck PASS; universal boundary gate unchanged for the 2 universal packages (adapter is Node-only by design); 15 test files/143 tests PASS; no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions. Focused adapter suite: 2 files/15 tests PASS.
  - Public API changed: yes — new `@nusajs/adapter-node` package exporting `createNodeServer()`, `NodeServer`, `NodeServerOptions`, `NodeListenResult`, `NodeListenOptions`; governed by Accepted ADR-026 adapter rules.
  - Security impact: high and positive — establishes the first host request boundary: raw-path preservation, host validation, forwarded-header gating, input limits, abort propagation, redacted failures, and bounded shutdown. Remaining: deadlines, per-header request size enforcement for chunked bodies, rate-limit hooks, and static/other adapters belong to FW-118/FW-119/FW-121/FW-122/FW-214.
- [x] **FW-114 [DONE/P0] Vite plugin skeleton** — depends FW-103, FW-107; completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Implemented private pre-enforced `createNusaVitePlugin()` with static config parsing, secure route scanning/parsing, deterministic route-manifest and typed-route virtual modules, generated TypeScript transform, and Vite watch registration; no application/config module execution and no network middleware/RPC/HMR claim.
  - Acceptance evidence: two identical real Vite builds byte-identical PASS; route edit rebuild updates virtual output PASS; hostile route body is never executed PASS; config diagnostics retain exact path and omit secret value PASS; relative root and config/routes traversal fail closed PASS; strict plugin type tests and exact package dependency/import boundary PASS.
  - Dependency review: exact `vite@7.3.6`, MIT, actively maintained, already lock-resolved through Vitest; large private build-time supply-chain surface remains covered by audit/license/lockfile gates.
  - Public API evidence: Accepted ADR-028, TSDoc, compiler root exports, type tests, README; compiler remains private and universal/runtime packages remain Vite-free.
  - Security impact: high and positive — adds a filesystem/build-input trust boundary with lexical and realpath containment, secret-safe static config parsing, internal virtual IDs, and no new network boundary.
- [x] **FW-115 [DONE/P0] CLI dev/build/preview** — depends FW-113, FW-114; completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Outcome: added private Node-only `@nusajs/cli` and provisional `nusajs` binary with deterministic `dev`, `build`, `preview`, and help contracts; programmatic `runCli()` is injectable/testable and never terminates the process.
  - Acceptance evidence: invalid/help/root parsing PASS; compiler Vite plugin wiring PASS; source maps disabled by default and explicit opt-in PASS; loopback default and explicit non-loopback `NUSA-CLI-0002` warning PASS; preview uses existing Vite artifacts without compiler plugin PASS; failures redact causes/secrets PASS; server lifecycle returned to caller PASS; strict public type tests and private package/bin/import boundaries PASS.
  - Security findings: no child process or environment-forwarding boundary introduced (SEC-SECRET-005); no artifact upload and source maps opt-in only (SEC-SECRET-004); rate limiting remains FW-206 and renderer trust boundary is unchanged; network exposure remains warned/experimental until FW-121 Host/origin/session hardening.
  - Verification: strict repository build/type tests PASS; universal boundary 2 packages PASS; 25 files/260 runtime tests PASS; high-severity audit PASS; 68-package license gate PASS.
  - Public API/ADR: private experimental package and binary documented by accepted ADR-029; no stable public API claim.
- [x] **FW-116 [DONE/P0] Kernel end-to-end fixture** — depends FW-108, FW-115; completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Outcome: added a real kernel fixture connecting non-executing route discovery, graph parsing, deterministic manifests, generated typed hrefs, matcher dispatch, request handling, Preact SSR, and the Node HTTP transport.
  - Acceptance evidence: hostile route modules remain unexecuted; generated route IDs and encoded dynamic values PASS; dynamic page, endpoint-first, HEAD body omission, 404, encoded-separator rejection, and invalid-Host rejection PASS over real HTTP.
  - Verification: strict repository build/type tests PASS; universal boundary 2 packages PASS; 26 files/261 runtime tests PASS; high-severity audit PASS; 68-package license gate PASS.
  - Public API changed: no. Security impact: low — adds executable cross-subsystem evidence for existing parser, URL normalization, rendering, and transport controls without a new trust boundary.
- [x] **FW-117 [DONE/P0] Universal package boundary scanner** — depends FW-101; completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Added a dependency-free, TypeScript-AST-based repository gate over explicit universal packages and both `src` and `dist` trees; Node tooling packages remain explicitly outside the universal set.
  - Rejects bare and `node:` built-ins across static imports/exports, import types, dynamic imports, and CommonJS `require()` with stable `NUSA_BOUNDARY_NODE_BUILTIN` diagnostics that disclose identifiers and paths but no source values.
  - Acceptance evidence: repository positive scan; Web Standard/package-import fixture; source/output negative fixtures; deterministic diagnostics; direct `boundaries:check` integration before the test suite in `verify`.
  - Verification: `pnpm run verify` PASS — format/lint PASS; strict build/typecheck PASS; universal boundary gate PASS for 2 packages; 11 test files/77 tests PASS; no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions.
  - Public API changed: no. Security impact: medium — adds fail-closed enforcement for NFR-001 and the source/built-output evidence required by the security workflow; future universal packages must be registered explicitly in the scanner.
- [x] **FW-118 [DONE/P0] URL normalization and malicious URL corpus** — depends FW-106 (DONE), FW-008 (DONE); completed 2026-08-17, agent: Freebuff (`buffy`)
  - Outcome: added the shared raw-pathname corpus (`packages/core/tests/malicious-url-corpus.ts`) as the single reference for SEC-INPUT-003/AC-ROUTE-07 — 42 named cases covering structural violations, malformed/hostile percent escapes, encoded separators, dot-segment traversal, raw controls, valid UTF-8/encodings, and resource bounds — plus a matcher conformance suite and the first official adapter conformance suite that runs the corpus through the real `@nusajs/adapter-node` HTTP server and asserts the same normalization decisions plus transport-boundary rejection of non-origin-form targets.
  - Acceptance criteria: corpus covers encoded slash/backslash, double-encoded, dot-dot, traversal, overlong UTF-8, lone surrogate, duplicate/trailing slash, literal backslash, query/fragment, protocol-relative, 8 KiB, and 256-segment categories PASS; every mustReject case fails closed against a catch-all matcher PASS; every valid case matches PASS; adapter conformance reproduces the matcher decision for every transportable case (200 iff the split pathname matches, else 4xx) PASS; absolute-form and protocol-relative targets rejected 400 at the transport boundary PASS; documented matcher-only cases (empty, no-slash, raw controls, raw UTF-8) remain covered by the matcher suite PASS.
  - Verification: `pnpm run verify` PASS — format/lint clean over 79 files; strict build/typecheck PASS; 17 test files/192 tests PASS (49 corpus tests); no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions.
  - Public API changed: no — corpus is a test fixture; adapter behavior unchanged.
  - Security impact: high and positive — establishes the shared reference corpus that future adapters (Bun/Deno/edge/static) must pass unchanged, satisfying SEC-INPUT-003's identical-normalization requirement and the AC-ROUTE-07 remaining risk from FW-106. Remaining: AC-ROUTE-06 10,000-route performance fixture (later benchmark work) and filesystem-root conformance FW-119.
- [x] **FW-119 [DONE/P0] Filesystem root, traversal, and symlink controls** — depends FW-104, FW-115; completed 2026-08-17, agent: GitHub Copilot (`gpt-pro`)
  - Outcome: framework-managed application roots, config files, route roots, route directories, and route files now reject symlink/junction aliases fail-closed; lexical and canonical containment is enforced before compiler reads, watches, or scans.
  - Acceptance evidence: linked application/config/route roots, escaping/internal directory links, route-file links, traversal overrides, deterministic scanner diagnostics, and host-path/cause redaction PASS on the Windows evidence host; conditional file-link creation only skips when host privileges prohibit creating the fixture.
  - Verification: strict repository build/type tests PASS; universal boundary 2 packages PASS; 26 files/265 runtime tests PASS; high-severity audit PASS; 68-package license gate PASS.
  - Public API changed: no. Security impact: high — hardens the project-files trust boundary for `SEC-INPUT-004`; pathname replacement races remain a documented local-machine residual risk. Static serving, source viewing, HMR, inspector access, and output-root policy remain assigned to later tasks.
  - ADR: proposed ADR-030 documents the v0.x fail-closed policy and supersede requirements.
- [x] **FW-120 [DONE/P0] Canary-secret scanner infrastructure** — depends FW-107, FW-114; completed 2026-08-18, agent: GitHub Copilot (`gpt-pro`)
  - Outcome: add dependency-free exact-byte canary scanning over final production artifacts before build success, with bounded no-follow traversal and redacted stable diagnostics.
  - Acceptance evidence: non-empty unique printable-ASCII canaries are defensively copied PASS; final JS/CSS/HTML/JSON/maps/binary bytes are scanned PASS; missing/unreadable/linked/non-regular/oversized output fails closed PASS; source-map-only canary PASS; findings expose no canary, excerpt, filename, offset, host path, or cause PASS; direct Vite build rejects before resolve and therefore before CLI success PASS; no scanner suppression, bypass, or dependency added PASS.
  - Verification: `pnpm run verify` PASS — format/lint clean over 119 files; strict build/typecheck PASS; universal boundary 2 packages PASS; 27 files/273 runtime tests PASS; no known vulnerabilities; 68-package license gate PASS.
  - Public API changed: no stable API — private compiler option/type only. Security impact: high and positive — adds bounded final-byte production artifact inspection for explicit canaries; arbitrary credential discovery, provider push protection, client/server taint analysis, and runtime log/error redaction remain separate controls.
  - Proposed ADR: ADR-031 selects private compiler ownership, fixed limits, exact-byte matching, post-enforced final-output lifecycle, and `NUSA-SECURITY-0002`.
- [ ] **FW-121 [BLOCKED/P0] Development-server host/origin/session security** — depends FW-115 (DONE); blocked 2026-08-18, agent: GitHub Copilot (`gpt-pro`)
  - Intended outcome: establish CLI-owned loopback/exposure, exact Host/Origin checks, unguessable per-server session authentication, dev-endpoint resource limits, and secret-safe diagnostics without adding a stable public API.
  - Derived acceptance criteria: loopback default and dedicated explicit exposure; post-bind accessible-URL warning without credentials; malformed/duplicate/unapproved Host denial; exact Origin and fresh-session authentication for exposed privileged dev transports; secure-random failure refuses exposure; retained source/stack endpoints pass traversal/symlink controls; payload/connection/message-rate limits reject and recover; shutdown releases all per-server state; no token or dev-session code reaches production artifacts.
  - Blocker: the approved security coverage map assigns `SEC-DEV-005` development payload, connection, and message-rate limits to FW-121, while Accepted ADR-029 and the FW-115 completion record defer rate limiting to FW-206. Repository rules prohibit implementation while normative authorities conflict.
  - Proposed ADR: ADR-032 recommends FW-121 own development transport limits and FW-206 own application middleware/rate limiting; it also specifies CLI ownership, exact Host/Origin policy, 256-bit per-session credentials using the existing secure-token policy, no URL/log credential transport, fail-closed Vite integration, and the FW-307/FW-308 boundary.
  - Maintainer decision required: accept, amend, or reject ADR-032 and explicitly resolve the ADR-029 ownership conflict. No `SEC-DEV-*` control is claimed complete.
- [ ] **FW-122 [PROPOSED/P0] Request limits, deadlines, abort, and cleanup** — depends FW-110, FW-113
- [x] **FW-123 [DONE/P0] Secure random and cryptography policy enforcement** — depends FW-101 (DONE); ADR-024 accepted by maintainer direction 2026-08-17; completed 2026-08-17, agent: Freebuff (`buffy`)
  - Outcome: implemented Accepted ADR-024 Option A in `@nusajs/core` — public `createSecureToken()` generating a fixed 32-byte / 256-bit token from `globalThis.crypto.getRandomValues` encoded as 43-character unpadded base64url by a package-internal deterministic encoder, failing closed with `NUSA-SECURITY-0001` when Web Crypto is unavailable; extended the universal boundary scanner with a `NUSA_BOUNDARY_INSECURE_RANDOM` gate that rejects `Math.random` in universal runtime source and built output (SEC-CRYPTO-002).
  - Acceptance criteria: Web Crypto is the only production source PASS; exactly 32 bytes requested per token PASS; 43-character unpadded base64url output PASS; no modulo bias (direct 6-bit grouping) PASS; unavailable/absent Web Crypto refuses without fallback PASS; deterministic test paths are package-internal and not exported (deep imports rejected) PASS; scanner rejects `Math.random` in source and dist fixtures PASS; repository scan stays clean PASS; universal-boundary, type, API-report, audit, and license gates PASS.
  - Verification: `pnpm run verify` PASS — format/lint clean over 84 files; strict build/typecheck PASS; universal boundary gate PASS for 2 packages (now including the insecure-random check); 19 test files/220 tests PASS (7 new secure-token tests + 1 scanner test); no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions.
  - Public API changed: yes — one function at the core root; internal `base64url` encoder stays package-private.
  - Security impact: high and positive — SEC-CRYPTO-001/002 enforced with fail-closed behavior and a repository-wide predictable-randomness gate; SEC-CRYPTO-003 (algorithm/key-size documentation) recorded in ADR-024.
  - Blocked 2026-08-17: `SEC-CRYPTO-002` requires sufficient entropy but does not select an entropy floor, output encoding, public API shape, or deterministic-test seam. These are security-sensitive stable-contract decisions and cannot be invented by the implementation agent.
  - Proposed ADR: `docs/adr/ADR-024-secure-random-contract.md` recommends Web Crypto only, fixed 32-byte/256-bit randomness, unpadded base64url, fail-closed behavior, and no public deterministic provider injection.
  - Maintainer decision required: accept, amend, or reject ADR-024 before implementation. No cryptographic control is currently claimed or discharged.
- [x] **FW-124 [DONE/P1] Security-header merge primitives** — depends FW-110 (DONE); completed 2026-08-17, agent: Freebuff (`buffy`)
  - Outcome: added universal `createSecurityHeaders()`, `mergeSecurityHeaders()`, and `mergeSecurityHeadersStrict()` to `@nusajs/core` — explicit opt-in builder for CSP, HSTS, X-Content-Type-Options, Referrer-Policy, X-Frame-Options, Permissions-Policy, COOP/CORP, and Cache-Control with canonical serialization, line-break injection rejection, unknown-CSP-directive and unknown-permissions-feature rejection, and a deterministic parent/child merge that never silently weakens: scalar conflicts keep the stricter parent value and are reported, CSP merges union disjoint directives and report directive-widening conflicts, and the strict variant throws `NUSA-SECURITY-0001`.
  - Acceptance criteria: deterministic serialization of every helper PASS; canonical CSP/permissions ordering PASS; hostile line-break, empty, unknown-directive, negative/fractional HSTS, and semicolon-injection values fail closed PASS; identical-value merges keep one copy PASS; new child headers append PASS; scalar conflicts keep parent and report PASS (covers SEC-CACHE-004 Vary/Cache-Control conflicts); CSP disjoint directives union and directive conflicts keep parent + report PASS; child subset of parent directive compatible PASS; repeated merges deterministic PASS; strict variant throws on conflict PASS; TSDoc, type tests, README example, deterministic API report, package-boundary PASS.
  - Verification: `pnpm run verify` PASS — format/lint clean over 81 files; strict build/typecheck PASS; universal boundary gate PASS for 2 packages; 18 test files/212 tests PASS (20 new security-header tests); no known high vulnerabilities; license gate PASS for 68 packages across 6 expressions.
  - Public API changed: yes — three functions and nine types at the existing core root, continuing the security-manifest direction of Accepted ADR-008/021.
  - Security impact: medium and positive — first composable security-header surface (SEC-HEADER-001/002); merge conflicts surface instead of silently weakening (SEC-HEADER-002, SEC-CACHE-004). Full pipeline policy application across normal/error/streamed/static responses (SEC-HEADER-003) and cookie primitives remain FW-206/FW-218 territory.

M1 exit: all M1 P0 tasks are DONE; B01/B03 smoke passes; security evidence exists for URL, filesystem, secrets, development server, request isolation, and resource limits.

## M2 — Application fundamentals

- [ ] **FW-201 [PROPOSED/P0] Nested layouts** — depends FW-116
- [ ] **FW-202 [PROPOSED/P0] Error and not-found boundaries** — depends FW-201
- [ ] **FW-203 [PROPOSED/P0] Loaders and typed data** — depends FW-201
- [ ] **FW-204 [PROPOSED/P0] Actions and progressive forms** — depends FW-203
- [ ] **FW-205 [PROPOSED/P0] API endpoints and methods** — depends FW-110
- [ ] **FW-206 [PROPOSED/P0] Middleware lifecycle and authorization hooks** — depends FW-110
- [ ] **FW-207 [PROPOSED/P0] Safe redirects and rewrites** — depends FW-206
- [ ] **FW-208 [BLOCKED/P0] Metadata/head contract** — depends FW-112
  - Blocked 2026-08-17: normative documents require typed metadata but do not define tag representation, nested precedence, deduplication identity, deterministic order, URL handling, or whether script/raw HTML contexts are representable.
  - Proposed ADR: `docs/adr/ADR-025-metadata-head-contract.md` recommends a closed renderer-neutral structured model, deterministic singleton/keyed parent-to-child merges, and exclusion of raw HTML/executable script.
  - Maintainer decision required: accept, amend, or reject ADR-025 before introducing the public API. Security impact: high because metadata crosses the server-to-browser XSS boundary.
- [ ] **FW-209 [PROPOSED/P1] Asset manifest, MIME, and serving controls** — depends FW-114
- [ ] **FW-210 [PROPOSED/P0] Static generation engine** — depends FW-112, FW-203
- [ ] **FW-211 [PROPOSED/P0] Static adapter** — depends FW-210
- [ ] **FW-212 [PROPOSED/P0] Environment schema and client-secret guard** — depends FW-114, FW-120
- [ ] **FW-213 [PROPOSED/P0] Serialization v1 and XSS corpus** — depends FW-203
- [ ] **FW-214 [PROPOSED/P0] Node/static conformance suite** — depends FW-113, FW-211
- [ ] **FW-215 [PROPOSED/P1] Project generator** — depends FW-115
- [ ] **FW-216 [PROPOSED/P1] First-app tutorial and compile tests** — depends FW-215
- [ ] **FW-217 [PROPOSED/P0] Production error and log redaction** — depends FW-202, FW-120
- [ ] **FW-218 [PROPOSED/P1] Secure cookie primitives** — depends FW-109, FW-110; created 2026-08-16 by FW-009 to own `SEC-AUTH-003`, which no task covered (FW-008 finding F-6)
  - AC: AC-1 cookie helpers default to `HttpOnly`, `Secure`, and an explicit `SameSite` with no implicit fallback; AC-2 attribute and prefix handling is tested against hostile names, values, domains, and paths; AC-3 a caller cannot weaken a default without naming it explicitly at the call site; AC-4 no cookie value reaches a log or diagnostic sink, verified against the FW-120 canary scanner.

## M3 — Client experience

- [ ] **FW-301 [PROPOSED/P0] Minimal client navigation protocol ADR** — depends FW-203, FW-213; required for v0.1
- [ ] **FW-302 [PROPOSED/P0] Client router runtime** — depends FW-301
- [ ] **FW-303 [PROPOSED/P0] Link interception and safe fallback** — depends FW-302
- [ ] **FW-304 [PROPOSED/P0] Focus, scroll, and history restoration** — depends FW-303
- [ ] **FW-305 [PROPOSED/P1] Conservative prefetch strategies** — depends FW-303
- [ ] **FW-306 [PROPOSED/P0] Hydration runtime** — depends FW-003, FW-213
- [ ] **FW-307 [PROPOSED/P0] Safe hydration mismatch diagnostics** — depends FW-306, FW-102
- [ ] **FW-308 [PROPOSED/P0] Secure component/loader/route HMR** — depends FW-114, FW-121, FW-203
- [ ] **FW-309 [PROPOSED/P1] Route and security inspector** — depends FW-107, FW-203
- [ ] **FW-310 [PROPOSED/P0] Browser E2E and security matrix** — depends FW-304, FW-306

## M4 — Progressive rendering

- [ ] **FW-401 [PROPOSED/P0] Streaming renderer contract** — depends FW-111, FW-213
- [ ] **FW-402 [PROPOSED/P0] Backpressure, limits, and abort propagation** — depends FW-401, FW-122
- [ ] **FW-403 [PROPOSED/P0] Island syntax ADR** — depends FW-306
- [ ] **FW-404 [PROPOSED/P0] Island compiler entries** — depends FW-403, FW-114
- [ ] **FW-405 [PROPOSED/P0] Hydration strategies** — depends FW-404
- [ ] **FW-406 [PROPOSED/P0] CSP nonce and secure bootstrap** — depends FW-401, FW-405, FW-124
- [ ] **FW-407 [PROPOSED/P1] Bundle and trust-boundary inspector** — depends FW-404
- [ ] **FW-408 [PROPOSED/P0] Streaming/island security conformance** — depends FW-402, FW-405, FW-406

## M5 — Data platform

- [ ] **FW-501 [PROPOSED/P0] Server-function transport ADR** — depends FW-204, FW-213
- [ ] **FW-502 [PROPOSED/P0] Server-function compiler and runtime** — depends FW-501
- [ ] **FW-503 [PROPOSED/P0] Runtime schema adapter and structured errors** — depends FW-502
- [ ] **FW-504 [PROPOSED/P0] CSRF, origin, authorization, and idempotency controls** — depends FW-204, FW-502
- [ ] **FW-505 [PROPOSED/P0] Cache contract ADR** — depends FW-203, FW-008
- [ ] **FW-506 [PROPOSED/P0] Route response cache** — depends FW-505
- [ ] **FW-507 [PROPOSED/P0] Cached-function primitive** — depends FW-505
- [ ] **FW-508 [PROPOSED/P0] Path/tag invalidation** — depends FW-506, FW-507
- [ ] **FW-509 [PROPOSED/P0] Cache correctness and security conformance** — depends FW-508
- [ ] **FW-510 [PROPOSED/P1] Redacted observability hooks** — depends FW-110, FW-217, FW-506

## M6 — Ecosystem and portability

- [ ] **FW-601 [PROPOSED/P0] Plugin API and trust-model RFC** — depends FW-114, FW-510
- [ ] **FW-602 [PROPOSED/P0] Plugin lifecycle and output-root controls** — depends FW-601
- [ ] **FW-603 [PROPOSED/P0] Plugin conformance and security kit** — depends FW-602
- [ ] **FW-604 [PROPOSED/P1] Bun adapter** — depends FW-214
- [ ] **FW-605 [PROPOSED/P1] Deno adapter** — depends FW-214
- [ ] **FW-606 [PROPOSED/P1] Edge adapter reference** — depends FW-214, FW-402
- [ ] **FW-607 [PROPOSED/P1] Privacy-safe OpenTelemetry plugin** — depends FW-510, FW-602
- [ ] **FW-608 [BLOCKED/P1] Codemod infrastructure** — depends FW-102
  - Blocked 2026-08-17: roadmap documents provide no transform contract, parser/language scope, dry-run and rollback semantics, filesystem/root/symlink controls, CLI integration, diagnostic codes, or acceptance fixtures. Starting it would introduce a new tooling architecture and filesystem trust boundary without PRD/ADR authority.
  - Maintainer decision required: define the first migration/use case and approve an ADR covering transform API, safety model, and verification before implementation.
- [ ] **FW-609 [PROPOSED/P1] Plugin and adapter author guides** — depends FW-603

## M7 — v1 production hardening

- [ ] **FW-701 [PROPOSED/P0] Public API and security surface audit** — depends every P0 task in M0–M6; this milestone predicate must be expanded to explicit task IDs before FW-701 becomes `READY`
- [ ] **FW-702 [PROPOSED/P0] Compatibility and deprecation policy** — depends FW-701
- [ ] **FW-703 [PROPOSED/P0] Independent security audit and remediation** — depends FW-701
- [ ] **FW-704 [PROPOSED/P0] Dedicated benchmark publication** — depends FW-701
- [ ] **FW-705 [PROPOSED/P0] Production deployment and security guides** — depends FW-214, FW-604, FW-605, FW-606
- [ ] **FW-706 [PROPOSED/P0] Production design-partner validation** — depends FW-702, FW-705
- [ ] **FW-709 [BLOCKED/P0] Vulnerability reporting and supported-version policy** — depends FW-008 (DONE); promoted to `READY` 2026-08-17 under delegated autonomy, started and blocked 2026-08-17, agent: GitHub Copilot (`gpt-pro`); blocker: a core maintainer must enable GitHub Private Vulnerability Reporting for `onenotdev/nusajs` and confirm the private advisory form is reachable—both the feature endpoint and form returned 404 on 2026-08-17, so AC-1 cannot truthfully pass; created 2026-08-16 by FW-009 to own the `AC-SEC-10` artifacts that no task named (FW-008 finding F-10)
  - AC: AC-1 a public `SECURITY.md` exists with a private reporting channel; AC-2 supported release lines and end-of-support dates are published; AC-3 a severity rubric and coordinated-disclosure policy are published; AC-4 a reporter-credit method exists; AC-5 the internal response-window targets of `docs/09_SECURITY_PRD.md` section 24 are not published as guarantees.
- [ ] **FW-707 [PROPOSED/P0] v1 release candidate** — depends FW-702, FW-703, FW-704, FW-705, FW-706, FW-709
- [ ] **FW-708 [PROPOSED/P0] v1 stable release** — depends FW-707 and zero P0/P1 release blockers

